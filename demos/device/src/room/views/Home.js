import React from 'react';

const Home = () => {
  return (
    <div
      style={{
        display: 'flex',
        height: '90vh',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h1>Meeting Lobby</h1>
    </div>
  );
};

export default Home;
